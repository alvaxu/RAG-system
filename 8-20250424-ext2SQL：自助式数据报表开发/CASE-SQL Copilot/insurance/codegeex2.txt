# language: SQL
# /*
查询每种保险类型的保险金额的平均值、最大值和最小值。数据库中有一下数据表：
=====
数据表：客户信息表（CustomerInfo），对应字段：
保单号（PolicyNumber）
客户ID（CustomerID）
保险类型（InsuranceType）
保险金额（CoverageAmount）
保险期限（CoverageTerm）
保费（Premium）
缴费频率（PaymentFrequency）
保单状态（PolicyStatus）
受益人（Beneficiary）
投保日期（PolicyStartDate）
终止日期（PolicyEndDate）
保费支付状态（PremiumPaymentStatus）
附加险种（Riders）
免赔额（Deductible）
理赔历史（ClaimHistory）
投保人信息（PolicyholderInfo）
代理人信息（AgentInfo）
=====
数据表：保险信息表（InsuranceInfo），对应字段：
保单号（PolicyNumber）
客户ID（CustomerID）
保险类型（InsuranceType）
保险金额（CoverageAmount）
保险期限（CoverageTerm）
保费（Premium）
缴费频率（PaymentFrequency）
保单状态（PolicyStatus）
受益人（Beneficiary）
投保日期（PolicyStartDate）
终止日期（PolicyEndDate）
保费支付状态（PremiumPaymentStatus）
附加险种（Riders）
免赔额（Deductible）
理赔历史（ClaimHistory）
投保人信息（PolicyholderInfo）
代理人信息（AgentInfo）
=====
数据表：理赔信息表（ClaimInfo），对应字段：
理赔号（ClaimNumber）
保单号（PolicyNumber）
客户ID（CustomerID）
理赔日期（ClaimDate）
理赔类型（ClaimType）
理赔金额（ClaimAmount）
理赔状态（ClaimStatus）
理赔描述（ClaimDescription）
受益人（Beneficiary）
医疗记录（MedicalRecords）
事故报告（AccidentReport）
审核人（ClaimHandler）
审核日期（ReviewDate）
支付方式（PaymentMethod）
支付日期（PaymentDate）
拒赔原因（DenialReason）
=====
数据表：保费信息表（PremiumInfo），对应字段：
客户ID（CustomerID）
保单号（PolicyNumber）
支付日期（PaymentDate）
支付金额（PaymentAmount）
支付方式（PaymentMethod）
保费类型（PremiumType）
缴费状态（PaymentStatus）
=====
数据表：受益人信息表（BeneficiaryInfo），对应字段：
受益人ID（BeneficiaryID）
保单号（PolicyNumber）
姓名（Name）
关系（Relationship）
联系地址（Address）
电话号码（PhoneNumber）
电子邮件（EmailAddress）
受益比例（BeneficiaryPercentage）
出生日期（DateOfBirth）
性别（Gender）
国籍（Nationality）
社会安全号码（SocialSecurityNumber）
=====
数据表：代理人信息表（AgentInfo），对应字段：
代理人ID（AgentID）
姓名（Name）
联系地址（Address）
电话号码（PhoneNumber）
电子邮件（EmailAddress）
证书号码（CertificateNumber）
执照发放日期（LicenseIssueDate）
执照到期日期（LicenseExpirationDate）
代理公司（AgencyName）
代理类型（AgentType）
执照状态（LicenseStatus）
销售区域（SalesRegion）
佣金结构（CommissionStructure）
=====
数据表：保险产品信息表（ProductInfo），对应字段：
产品ID（ProductID）
产品名称（ProductName）
产品类型（ProductType）
保险金额范围（CoverageRange）
保险期限（CoverageTerm）
保费（Premium）
缴费频率（PaymentFrequency）
产品特性（ProductFeatures）
投保年龄限制（AgeLimit）
保费计算方式（PremiumCalculation）
理赔流程（ClaimsProcess）
投保要求（UnderwritingRequirements）
销售区域（SalesRegion）
销售代理人（SalesAgent）
产品状态（ProductStatus）
=====
数据表：投保单信息表（ApplicationInfo），对应字段：
投保单号（ApplicationNumber）
客户ID（CustomerID）
保险产品ID（ProductID）
投保日期（ApplicationDate）
生效日期（EffectiveDate）
保险金额（CoverageAmount）
保险期限（CoverageTerm）
保费（Premium）
缴费频率（PaymentFrequency）
受益人（Beneficiary）
申请状态（ApplicationStatus）
理赔状态（ClaimStatus）
签名（Signature）
=====
数据表：保险公司内部员工表（EmployeeInfo），对应字段：
员工ID（EmployeeID）
姓名（Name）
性别（Gender）
出生日期（DateOfBirth）
联系地址（Address）
电话号码（PhoneNumber）
电子邮件（EmailAddress）
入职日期（HireDate）
职位（Position）
部门（Department）
工资（Salary）
工作地点（Location）
上级主管（Supervisor）
员工类型（EmployeeType）
员工状态（EmployeeStatus）
=====
数据表：保险费率表（RateTable），对应字段：
产品ID（ProductID）
年龄范围（AgeRange）
性别（Gender）
健康状况（HealthCondition）
费率类型（RateType）
费率数值（RateValue）
生效日期（EffectiveDate）
失效日期（ExpirationDate）
地理区域（Region）
职业（Occupation）
保险金额范围（CoverageRange）
缴费频率（PaymentFrequency）
*/
查询每种保险类型的保险金额的平均值、最大值和最小值。
